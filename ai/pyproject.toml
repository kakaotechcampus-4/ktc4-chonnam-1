[project]
name = "ai"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "httpx",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/ai"]
