# 파이프라인

정규화 → URL 추출 → 해석·분류 → 규범 검색 → 신호 평가 → 분기 → 필요 시 관측·대조·보강 → 결과 조립 순서다. URL이 0개 또는 여러 개면 입력을 다시 요청한다. 관측이 없으면 대조를 생략하고 해석은 null이다. 분기는 코드가 결정하며, 한도 소진 시 held로 끝낸다. 원문·관측·해석의 출처를 구분하고 URL 원본을 보존한다.
